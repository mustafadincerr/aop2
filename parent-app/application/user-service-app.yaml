apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: user-service
  namespace: vlog-app
spec:
  destination:
    namespace: vlog-app
    server: https://kubernetes.default.svc
  source:
    repoURL: https://github.com/mustafadincerr/aop2.git
    targetRevision: HEAD
    path: myapp1/user-service
  project: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
